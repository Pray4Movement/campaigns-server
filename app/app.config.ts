export default defineAppConfig({
  ui: {
    colors: {
      primary: 'forest',
      secondary: 'sage',
      neutral: 'beige'
    },
    button: {
      slots: {
        base: 'font-medium uppercase tracking-wide'
      }
    }
  }
})
