import type { FieldDefinition } from '../types'

export const field: FieldDefinition = {
  key: 'people_desc',
  labelKey: 'peopleGroups.fields.people_desc',
  type: 'textarea',
  category: 'basic',
  tableColumn: true,
  description: 'Imported from IMB CSV via PEID'
}
