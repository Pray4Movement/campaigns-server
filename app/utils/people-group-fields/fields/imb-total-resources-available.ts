import type { FieldDefinition } from '../types'

export const field: FieldDefinition = {
  key: 'imb_total_resources_available',
  labelKey: 'peopleGroups.fields.imb_total_resources_available',
  type: 'number',
  category: 'resources'
}
