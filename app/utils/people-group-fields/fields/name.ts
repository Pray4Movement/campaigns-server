import type { FieldDefinition } from '../types'

export const field: FieldDefinition = {
  key: 'name',
  labelKey: 'peopleGroups.fields.name',
  type: 'text',
  category: 'basic',
  tableColumn: true
}
