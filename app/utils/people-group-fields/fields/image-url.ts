import type { FieldDefinition } from '../types'

export const field: FieldDefinition = {
  key: 'image_url',
  labelKey: 'peopleGroups.fields.image_url',
  type: 'text',
  category: 'media',
  tableColumn: true
}
