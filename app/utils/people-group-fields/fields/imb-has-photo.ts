import type { FieldDefinition } from '../types'

export const field: FieldDefinition = {
  key: 'imb_has_photo',
  labelKey: 'peopleGroups.fields.imb_has_photo',
  type: 'boolean',
  category: 'media'
}
